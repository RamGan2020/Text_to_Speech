# Text-to-Speech / Speech-to-Text Web Application

Web-приложение для синтеза речи (TTS) и распознавания речи (STT) с использованием нейросетевых моделей Silero TTS v4 и OpenAI Whisper. Позволяет озвучивать текст в MP3, распознавать загруженные аудиофайлы в текст, записывать речь с микрофона и сразу распознавать её, а также задавать вопросы AI DeepSeek и озвучивать ответы.

## Особенности

- ✅ Бесплатно и полностью локально (без внешних API, кроме DeepSeek)
- ✅ Нет ограничений на длину текста
- ✅ Русский язык (5 голосов: 3 женских, 2 мужских)
- ✅ Простой и понятный интерфейс с вкладками
- ✅ Автоматическая разбивка длинных текстов на чанки
- ✅ Воспроизведение и скачивание MP3
- ✅ Настройки TTS: выбор голоса, ударения, буква «ё»
- ✅ Распознавание MP3, WAV, OGG, FLAC в текст
- ✅ Запись речи с микрофона прямо в браузере
- ✅ Конвертация записи из webm в WAV без ffmpeg (через AudioContext)
- ✅ Копирование распознанного текста в буфер обмена
- ✅ **Интеграция с DeepSeek AI** — задайте вопрос и озвучьте ответ

## Архитектура

```
┌─────────────────┐         HTTP/JSON, multipart         ┌─────────────────┐
│   Frontend      │ ◄──────────────────────────────────► │   Backend       │
│   React + Vite  │                                       │   FastAPI       │
│   (порт 3000)   │                                       │   (порт 8000)   │
│                 │                                       │                 │
│  ┌───────────┐  │                                       │ ┌─────────────┐ │
│  │ TTS       │  │── текст ───────────────────► MP3 ────│▶│ Silero TTS  │ │
│  │           │  │◄── MP3 ──────────────────── текст ───│  │   v4        │ │
│  └───────────┘  │                                       │ └─────────────┘ │
│  ┌───────────┐  │                                       │ ┌─────────────┐ │
│  │ STT       │  │── аудио (файл) ───────────► текст ───│▶│ Whisper     │ │
│  │           │  │◄── текст ────────────────── аудио ───│  │   small     │ │
│  └───────────┘  │                                       │ └─────────────┘ │
│  ┌───────────┐  │                                       │                 │
│  │ Mic Rec   │  │  Запись с микрофона (MediaRecorder)  │                 │
│  │           │  │  Конвертация webm→WAV (AudioContext) │                 │
│  │           │  │── WAV ────────────────────► текст ───│▶│               │
│  └───────────┘  │                                       │                 │
│  ┌───────────┐  │                                       │ ┌─────────────┐ │
│  │ DeepSeek  │  │── вопрос ─────────────────► ответ ───│▶│ DeepSeek    │ │
│  │           │  │◄── ответ ────────────────── вопрос ──│  │   API       │─│──► https://api.deepseek.com
│  └───────────┘  │                                       │ └─────────────┘ │
│                 │                                       │                 │
│  ┌───────────┐  │                                       │                 │
│  │ Озвучка   │  │── ответ DeepSeek ──────────► MP3 ────│▶│ Silero TTS  │ │
│  │ ответа    │  │◄── MP3 ──────────────────── ответ ───│  │   v4        │ │
│  └───────────┘  │                                       │ └─────────────┘ │
└─────────────────┘                                       └─────────────────┘
```

## Стек технологий

### Backend
| Пакет | Версия | Назначение |
|-------|--------|------------|
| Python | 3.8+ | Язык программирования |
| FastAPI | 0.104.1 | Асинхронный веб-фреймворк |
| Uvicorn | 0.24.0 | ASGI-сервер |
| PyTorch | 2.1.1 | Фреймворк нейросетей |
| Torchaudio | 2.1.1 | Обработка аудио (сохранение MP3) |
| Silero TTS v4 | - | Модель синтеза речи |
| OpenAI Whisper | 20231117 | Модель распознавания речи |
| librosa | 0.9.2 | Загрузка и ресемплинг аудио |
| soundfile | 0.12.1 | Чтение/запись аудиофайлов (WAV) |
| openai | >=1.0.0 | SDK для DeepSeek API (совместим с OpenAI) |
| DeepSeek API | внешний | AI-модель для генерации ответов |

### Frontend
| Пакет | Версия | Назначение |
|-------|--------|------------|
| React | 18.2 | UI-библиотека |
| Vite | 5.0 | Сборщик |
| React-Bootstrap | 2.9 | Компоненты |
| Bootstrap | 5.3 | Стили |
| MediaRecorder API | (браузер) | Запись аудио с микрофона |
| AudioContext API | (браузер) | Конвертация webm → WAV |

## Структура проекта

```
Text_to_Speech/
├── backend/
│   ├── main.py              # FastAPI сервер, эндпоинты (TTS + STT + DeepSeek)
│   ├── tts_service.py       # Сервис Silero TTS (загрузка, синтез)
│   ├── stt_service.py       # Сервис Whisper (распознавание речи)
│   ├── deepseek_service.py  # Сервис DeepSeek AI (вопрос-ответ)
│   └── requirements.txt     # Python зависимости
├── frontend/
│   ├── src/
│   │   ├── App.jsx          # Главный React-компонент (TTS + STT + Mic)
│   │   ├── App.css          # Пользовательские стили + анимация записи
│   │   └── main.jsx         # Точка входа React
│   ├── index.html           # HTML-шаблон
│   ├── package.json         # npm зависимости
│   └── vite.config.js       # Конфигурация Vite
├── docs/
│   ├── DOCUMENTATION.md     # Полная документация (этот файл)
│   └── LINUX_DEPLOY.md      # Инструкция по деплою на Linux
├── .gitignore               # Исключения для Git
└── README.md                # Краткое описание проекта
```

## API-документация

### POST /synthesize

Синтез речи из текста (TTS).

**Request body (JSON):**
```json
{
  "text": "Пример текста для озвучивания",
  "speaker": "kseniya",
  "put_accent": true,
  "put_yo": true,
  "podcast_mode": false
}
```

| Поле | Тип | По умолчанию | Описание |
|------|-----|--------------|----------|
| text | string | — | Текст для озвучивания (обязательное) |
| speaker | string | kseniya | Голос: kseniya, xenia, baya, aidar, eugene |
| put_accent | boolean | true | Расстановка ударений |
| put_yo | boolean | true | Замена «е» на «ё» |
| podcast_mode | boolean | false | Включает режим подкаста с тегами `[voice:...]` и `[pause:...]` |

**Response:** MP3-файл (`audio/mpeg`)

**Ошибки:**
- `400` — пустой текст
- `500` — ошибка синтеза

---

### POST /transcribe

Распознавание речи из аудио-файла (STT).

**Request (multipart/form-data):**
```
audio: файл (MP3, WAV, OGG, FLAC, WEBM)
```

**Response (JSON):**
```json
{
  "text": "Распознанный текст из аудио",
  "filename": "записанная_речь.mp3"
}
```

**Ошибки:**
- `400` — файл не загружен или неподдерживаемый формат
- `500` — ошибка распознавания

---

### GET /health

Проверка работоспособности.

**Response:** `{"status": "ok"}`

---

### POST /ask_deepseek

Получение ответа от DeepSeek AI.

**Request body (JSON):**
```json
{
  "question": "Что такое искусственный интеллект?",
  "api_key": "sk-...",
  "system_prompt": "Ты — полезный ассистент.",
  "model": "deepseek-chat",
  "max_tokens": 2048,
  "temperature": 0.7
}
```

| Поле | Тип | По умолчанию | Описание |
|------|-----|--------------|----------|
| question | string | — | Текст вопроса (обязательное) |
| api_key | string | из окружения | API-ключ DeepSeek (или переменная DEEPSEEK_API_KEY) |
| system_prompt | string | — | Системная инструкция (роль, стиль ответов) |
| model | string | deepseek-chat | Модель: deepseek-chat или deepseek-reasoner |
| max_tokens | integer | 2048 | Максимальное количество токенов в ответе |
| temperature | float | 0.7 | Температура генерации (0.0–1.0) |

**Response (JSON):**
```json
{
  "answer": "Искусственный интеллект — это...",
  "model": "deepseek-chat"
}
```

**Ошибки:**
- `400` — пустой вопрос, неверный API-ключ
- `500` — ошибка API (лимиты, недоступность сервиса)

---

### POST /ask_and_synthesize

Комбинированный запрос: вопрос к DeepSeek → озвучка ответа.

**Request body (JSON):**
```json
{
  "question": "Расскажи коротко о Python.",
  "api_key": "sk-...",
  "speaker": "kseniya",
  "put_accent": true,
  "put_yo": true,
  "model": "deepseek-chat"
}
```

| Поле | Тип | По умолчанию | Описание |
|------|-----|--------------|----------|
| question | string | — | Текст вопроса (обязательное) |
| api_key | string | из окружения | API-ключ DeepSeek |
| system_prompt | string | — | Системная инструкция |
| speaker | string | kseniya | Голос для озвучки |
| put_accent | boolean | true | Расстановка ударений |
| put_yo | boolean | true | Замена «е» на «ё» |
| model | string | deepseek-chat | Модель DeepSeek |
| max_tokens | integer | 2048 | Макс. токенов |
| temperature | float | 0.7 | Температура |

**Response:** MP3-файл (`audio/mpeg`) + заголовок `X-DeepSeek-Answer` с текстом ответа

**Ошибки:**
- `400` — пустой вопрос, неверный ключ
- `500` — ошибка API или синтеза

## Как получить API-ключ DeepSeek

1. Зарегистрируйтесь на [platform.deepseek.com](https://platform.deepseek.com)
2. Перейдите в раздел API Keys
3. Создайте новый ключ
4. Скопируйте ключ и вставьте в поле «API-ключ DeepSeek» на вкладке «DeepSeek AI»

> **Примечание:** DeepSeek предоставляет бесплатный лимит запросов. Для уточнения тарифов обратитесь к документации DeepSeek.

## Доступные голоса (TTS)

| Голос | Пол | Описание |
|-------|-----|----------|
| kseniya | Женский | Основной женский голос |
| xenia | Женский | Альтернативный женский |
| baya | Женский | Другой тембр женского |
| aidar | Мужской | Мужской голос |
| eugene | Мужской | Другой тембр мужского |

## Поддерживаемые форматы аудио (STT)

| Формат | Загрузка файла | Запись с микрофона | Примечание |
|--------|---------------|-------------------|------------|
| MP3 | ✅ | ❌ | Стандартный формат |
| WAV | ✅ | ✅ | Без сжатия |
| OGG | ✅ | ❌ | Сжатый формат |
| FLAC | ✅ | ❌ | Lossless сжатие |
| WEBM | ✅ | ⚠️ | Конвертируется в WAV в браузере |

**Максимальный размер файла:** 25 МБ

## Настройки качества (TTS)

| Параметр | Описание |
|----------|----------|
| put_accent | Расстановка ударений — улучшает произношение сложных слов |
| put_yo | Автоматическая замена «е» на «ё» — корректное произношение |

## Podcast Mode (TTS)

`podcast_mode` включает специальный разбор тегов в тексте и позволяет:
- переключать голос по ходу текста;
- вставлять паузы заданной длины.

### Поддерживаемые теги

- `[voice:kseniya]`, `[voice:xenia]`, `[voice:baya]`, `[voice:aidar]`, `[voice:eugene]`
- `[pause:1.5]` или `[pause:1.5s]` — пауза в секундах (можно дробное значение)
- `[pause:500ms]` — пауза в миллисекундах

### Пример текста

```text
[voice:kseniya] Всем привет! Это наш подкаст.
[pause:0.8]
[voice:aidar] Сегодня обсудим новости по проекту.
[pause:1.2]
[voice:kseniya] Поехали!
```

### Использование в UI

1. Откройте вкладку «Текст в речь».
2. Включите переключатель «Режим подкаста (теги [voice:] и [pause:])».
3. В блоке «Примеры для вставки» нажмите нужный шаблон (он вставится в поле текста).
4. Нажмите «Обработать» для синтеза.

## Как работает синтез речи (TTS)

1. Пользователь вводит текст → нажимает «Обработать»
2. Frontend отправляет POST-запрос с настройками на `/synthesize`
3. Backend разбивает текст на чанки (до 500 символов)
4. Для каждого чанка модель Silero TTS v4 генерирует аудио-тензор
5. Тензоры объединяются в один аудио-поток
6. Аудио сохраняется как MP3 и отправляется обратно
7. Frontend показывает плеер и кнопку скачивания

## Как работает распознавание загруженного файла (STT)

1. Пользователь выбирает аудио-файл → нажимает «Распознать файл»
2. Frontend отправляет файл через `FormData` на `/transcribe`
3. Backend сохраняет файл во временный файл (для корректного определения формата)
4. librosa загружает аудио, конвертирует в моно
5. Аудио ресемплится до 16kHz через librosa.resample
6. Данные передаются в Whisper как numpy массив (float32)
7. Whisper распознаёт текст и возвращает результат
8. Frontend показывает текст в поле и кнопку «Копировать»

## Как работает запись с микрофона

1. Пользователь нажимает «Начать запись»
2. Браузер запрашивает разрешение на доступ к микрофону
3. `MediaRecorder API` начинает запись в формате `audio/webm`
4. Отображается пульсирующий индикатор и таймер длительности
5. При нажатии «Остановить»:
   - `AudioContext.decodeAudioData()` декодирует webm в AudioBuffer
   - Функция `audioBufferToWav()` конвертирует AudioBuffer → WAV Blob:
     - Интерливинг каналов (чередование левого/правого)
     - Конвертация float32 → int16
     - Ручная запись WAV заголовка (44 байта) + аудио-данные
6. Появляется аудиоплеер для прослушивания записи
7. При нажатии «Распознать запись» → отправка WAV на `/transcribe`
8. Результат отображается в поле с текстом

## Ограничения моделей

### Silero TTS v4
- Поддерживает тексты до ~500 символов за один вызов
- При длинных текстах сервис автоматически разбивает на чанки
- Паузы между предложениями сохраняются внутри чанка

### Whisper small
- Модель требует ~500 МБ оперативной памяти
- Распознавание длинных файлов может занять время
- Максимальный размер файла: 25 МБ
- M4A не поддерживается (требуется ffmpeg)

### Запись с микрофона
- Зависит от поддержки MediaRecorder API браузером
- Запись происходит в формате webm, конвертация в WAV — в браузере
- Требуется разрешение пользователя на доступ к микрофону
- Работает в Chrome, Firefox, Edge (не поддерживается в Safari < 14.1)
